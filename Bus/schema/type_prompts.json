{
  "$id": "BROWNEYE.TYPE.PROMPTS.v1",
  "type": "object",
  "required": ["payload"],
  "properties": {
    "payload": {
      "type": "object",
      "required": ["prompt_text", "target_module"],
      "properties": {
        "prompt_text": { "type": "string", "minLength": 1 },
        "target_module": { "type": "string", "minLength": 2 },
        "visibility": { "type": "string", "enum": ["private", "public"] }
      }
    }
  }
}
