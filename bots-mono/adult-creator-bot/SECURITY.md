# Security

- Never commit `.env` or tokens.
- Use environment variables for secrets.
- Optional Windows DPAPI secrets file can be set with `WINDOWS_SECRETS_PATH`.
- Rotate Discord tokens if exposed.
